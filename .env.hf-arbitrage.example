# =============================================================================
# Docker #5: 高频套利独立环境变量
# HF Arbitrage - Isolated Environment Variables
#
# ⚠️ 重要: 此实例必须使用独立的子账户和API Key!
# ⚠️ IMPORTANT: This instance MUST use isolated sub-account and API Keys!
# =============================================================================

# -----------------------------------------------------------------------------
# Binance 子账户 API (独立)
# Binance Sub-Account API (Isolated)
# -----------------------------------------------------------------------------
HF_BINANCE_API_KEY=your_hf_binance_api_key
HF_BINANCE_API_SECRET=your_hf_binance_api_secret

# -----------------------------------------------------------------------------
# OKX 子账户 API (独立)
# OKX Sub-Account API (Isolated)
# -----------------------------------------------------------------------------
HF_OKX_API_KEY=your_hf_okx_api_key
HF_OKX_API_SECRET=your_hf_okx_api_secret
HF_OKX_PASSPHRASE=your_hf_okx_passphrase

# -----------------------------------------------------------------------------
# Bybit 子账户 API (独立)
# Bybit Sub-Account API (Isolated)
# -----------------------------------------------------------------------------
HF_BYBIT_API_KEY=your_hf_bybit_api_key
HF_BYBIT_API_SECRET=your_hf_bybit_api_secret

# -----------------------------------------------------------------------------
# Gate.io 子账户 API (独立)
# Gate.io Sub-Account API (Isolated)
# -----------------------------------------------------------------------------
HF_GATE_API_KEY=your_hf_gate_api_key
HF_GATE_API_SECRET=your_hf_gate_api_secret

# -----------------------------------------------------------------------------
# 独立风控预算
# Isolated Risk Budget
# -----------------------------------------------------------------------------
HF_MAX_CAPITAL=10000
HF_MAX_DAILY_LOSS=300
HF_MAX_POSITION_VALUE=500
