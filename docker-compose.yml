# =============================================================================
# Docker Compose 配置
# Quant Trading System Infrastructure
# =============================================================================

version: '3.8'

services:
  # ===========================================================================
  # Quant Trading App - Shadow Mode (影子模式)
  # ===========================================================================
  quant-shadow:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: quant-trading-system:latest
    container_name: quant-trading-shadow
    restart: unless-stopped
    ports:
      - "3000:3000"   # API
      - "9091:9091"   # Metrics
      - "8080:8080"   # WebSocket
    volumes:
      # 日志持久化 / Log persistence
      - ./logs:/app/logs
      # 数据持久化 / Data persistence
      - ./data:/app/data
      # 配置挂载 / Config mount
      - ./config:/app/config:ro
      # 环境变量 / Environment file
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=production
      - RUN_MODE=shadow
      - LOG_LEVEL=info
      - REDIS_HOST=redis-master
      - REDIS_PORT=6379
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - TZ=Asia/Shanghai
    depends_on:
      redis-master:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - quant-network
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.25'
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "5"

  # ===========================================================================
  # Quant Trading App - Live Mode (实盘模式)
  # ===========================================================================
  quant-live:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: quant-trading-system:latest
    container_name: quant-trading-live
    restart: unless-stopped
    ports:
      - "3001:3000"   # API (different port)
      - "9092:9091"   # Metrics
      - "8081:8080"   # WebSocket
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./config:/app/config:ro
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=production
      - RUN_MODE=live
      - LOG_LEVEL=info
      - REDIS_HOST=redis-master
      - REDIS_PORT=6379
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - TZ=Asia/Shanghai
    depends_on:
      redis-master:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - quant-network
    profiles:
      - live
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    logging:
      driver: json-file
      options:
        max-size: "200m"
        max-file: "10"

  # ===========================================================================
  # Quant Trading App - Development Mode (开发模式)
  # ===========================================================================
  quant-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: quant-trading-dev
    ports:
      - "3000:3000"
      - "9091:9091"
      - "8080:8080"
    volumes:
      # 源码热重载 / Source hot reload
      - ./src:/app/src
      - ./config:/app/config
      - ./scripts:/app/scripts
      - ./tests:/app/tests
      - ./logs:/app/logs
      - ./data:/app/data
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=development
      - RUN_MODE=shadow
      - LOG_LEVEL=debug
      - REDIS_HOST=redis-master
      - REDIS_PORT=6379
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
      - clickhouse
    networks:
      - quant-network
    profiles:
      - dev

  # ===========================================================================
  # Redis Master - 内存数据库 (热数据)
  # ===========================================================================
  redis-master:
    image: redis:7-alpine
    container_name: quant-redis-master
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      # 数据持久化 / Data persistence
      - redis_master_data:/data
      # 自定义配置文件 / Custom config file
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf:ro
      # 备份目录 / Backup directory
      - ./backups/redis:/backups
    command: redis-server /usr/local/etc/redis/redis.conf
    environment:
      - TZ=Asia/Shanghai
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - quant-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  # ===========================================================================
  # Redis Replica 1 - 只读副本 (高可用)
  # ===========================================================================
  redis-replica-1:
    image: redis:7-alpine
    container_name: quant-redis-replica-1
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis_replica1_data:/data
    command: redis-server --replicaof redis-master 6379 --appendonly yes
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - quant-network
    profiles:
      - ha
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  # ===========================================================================
  # Redis Replica 2 - 只读副本 (高可用)
  # ===========================================================================
  redis-replica-2:
    image: redis:7-alpine
    container_name: quant-redis-replica-2
    restart: unless-stopped
    ports:
      - "6381:6379"
    volumes:
      - redis_replica2_data:/data
    command: redis-server --replicaof redis-master 6379 --appendonly yes
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - quant-network
    profiles:
      - ha
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  # ===========================================================================
  # Redis Sentinel 1 - 高可用监控
  # ===========================================================================
  redis-sentinel-1:
    image: redis:7-alpine
    container_name: quant-redis-sentinel-1
    restart: unless-stopped
    ports:
      - "26379:26379"
    volumes:
      - ./config/redis-sentinel/sentinel-1.conf:/etc/redis/sentinel.conf:ro
    command: redis-sentinel /etc/redis/sentinel.conf
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
      - redis-replica-1
      - redis-replica-2
    networks:
      - quant-network
    profiles:
      - ha

  # ===========================================================================
  # Redis Sentinel 2 - 高可用监控
  # ===========================================================================
  redis-sentinel-2:
    image: redis:7-alpine
    container_name: quant-redis-sentinel-2
    restart: unless-stopped
    ports:
      - "26380:26379"
    volumes:
      - ./config/redis-sentinel/sentinel-2.conf:/etc/redis/sentinel.conf:ro
    command: redis-sentinel /etc/redis/sentinel.conf
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
      - redis-replica-1
      - redis-replica-2
    networks:
      - quant-network
    profiles:
      - ha

  # ===========================================================================
  # Redis Sentinel 3 - 高可用监控
  # ===========================================================================
  redis-sentinel-3:
    image: redis:7-alpine
    container_name: quant-redis-sentinel-3
    restart: unless-stopped
    ports:
      - "26381:26379"
    volumes:
      - ./config/redis-sentinel/sentinel-3.conf:/etc/redis/sentinel.conf:ro
    command: redis-sentinel /etc/redis/sentinel.conf
    environment:
      - TZ=Asia/Shanghai
    depends_on:
      - redis-master
      - redis-replica-1
      - redis-replica-2
    networks:
      - quant-network
    profiles:
      - ha

  # ===========================================================================
  # ClickHouse - 分析型数据库 (冷数据)
  # ===========================================================================
  clickhouse:
    image: clickhouse/clickhouse-server:24-alpine
    container_name: quant-clickhouse
    restart: unless-stopped
    ports:
      - "8123:8123"   # HTTP 接口
      - "9000:9000"   # Native 接口
    volumes:
      # 数据持久化 / Data persistence
      - clickhouse_data:/var/lib/clickhouse
      # 日志 / Logs
      - clickhouse_logs:/var/log/clickhouse-server
      # 自定义配置 / Custom config
      - ./config/clickhouse:/etc/clickhouse-server/config.d:ro
    environment:
      - TZ=Asia/Shanghai
      - CLICKHOUSE_DB=quant_trading
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - quant-network
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

  # ===========================================================================
  # Redis Commander - Redis Web UI (可选)
  # ===========================================================================
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: quant-redis-commander
    restart: unless-stopped
    environment:
      - REDIS_HOSTS=master:redis-master:6379,replica1:redis-replica-1:6379,replica2:redis-replica-2:6379
      - HTTP_USER=admin
      - HTTP_PASSWORD=admin123
    ports:
      - "8081:8081"
    depends_on:
      - redis-master
    networks:
      - quant-network
    profiles:
      - tools

  # ===========================================================================
  # Grafana - 监控面板 (可选)
  # ===========================================================================
  grafana:
    image: grafana/grafana:latest
    container_name: quant-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./config/grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - redis-master
      - clickhouse
    networks:
      - quant-network
    profiles:
      - monitoring

  # ===========================================================================
  # Prometheus - 指标收集 (可选)
  # ===========================================================================
  prometheus:
    image: prom/prometheus:latest
    container_name: quant-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - prometheus_data:/prometheus
      - ./config/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
    networks:
      - quant-network
    profiles:
      - monitoring

# =============================================================================
# Networks / 网络配置
# =============================================================================
networks:
  quant-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

# =============================================================================
# Volumes / 数据卷
# =============================================================================
volumes:
  redis_master_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/redis-master

  redis_replica1_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/redis-replica1

  redis_replica2_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/redis-replica2

  clickhouse_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data/clickhouse

  clickhouse_logs:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./logs/clickhouse

  grafana_data:
    driver: local

  prometheus_data:
    driver: local

  app_logs:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./logs

  app_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data
